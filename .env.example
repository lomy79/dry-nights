# Copia questo file in `.env` e riempi con i valori del TUO progetto Supabase.
# Il file `.env` è ignorato da Git (vedi .gitignore) — non committare mai le chiavi.
#
# AMBIENTI: usa DUE progetti Supabase separati, uno per la produzione e uno per
# lo staging (vedi docs/deploy-vercel.md). In LOCALE punta sempre allo STAGING,
# così sviluppando non tocchi mai i dati reali di produzione.
#
# Dove trovarli: Supabase Dashboard -> Project Settings -> API Keys
#   - "API URL" (o "Project URL")       -> VITE_SUPABASE_URL
#   - Publishable key (sb_publishable_) -> VITE_SUPABASE_PUBLISHABLE_KEY
#
# USA LA PUBLISHABLE KEY (sb_publishable_...), non la secret (sb_secret_...):
# la secret bypassa le RLS e non deve MAI stare nel client.
# (Le vecchie chiavi anon/service_role funzionano ancora ma sono in dismissione.)
#
# La publishable key è pensata per il client. A proteggere i dati sono le
# Row-Level Security policy definite in docs/schema.sql, non il segreto.

VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# ---- Promemoria push ---------------------------------------------------------
# Chiave PUBBLICA VAPID: identifica il mittente dei push. È pubblica per natura
# (il browser la manda al push service), la privata resta nei secrets della Edge
# Function e non tocca mai il client.
#
# La stampa `./scripts/setup-notifiche.sh` a fine setup. Ogni progetto Supabase
# ha la SUA: quella di staging qui, quella di produzione nelle env di Vercel.
# Senza, l'app funziona ma le impostazioni dei promemoria restano disattivate.
#
# Dettagli e diagnostica: docs/notifiche-push.md
VITE_VAPID_PUBLIC_KEY=
