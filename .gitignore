# Dipendenze
node_modules/

# Build
dist/
dist-ssr/
*.local

# Segreti — le chiavi Supabase NON vanno in Git (vedi CLAUDE.md)
.env
.env.*
!.env.example
!.env.scripts.example
secret
secrets
*.secret

# Stato locale della CLI Supabase (ref dell'ultimo progetto su cui si è fatto
# deploy). È per macchina, e qui i progetti sono due: non deve finire in Git.
supabase/.temp/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log
npm-debug.log*

# Test / coverage
coverage/
